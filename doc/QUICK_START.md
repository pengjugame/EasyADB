# EasyADB 快速开始指南

本指南将帮助你在 5 分钟内开始使用 EasyADB。

## 目录

- [前置要求](#前置要求)
- [安装步骤](#安装步骤)
- [连接设备](#连接设备)
- [第一次使用](#第一次使用)
- [常用操作](#常用操作)
- [故障排除](#故障排除)

---

## 前置要求

### 1. Node.js

检查是否已安装 Node.js：

```bash
node --version
```

如果未安装，请访问 [nodejs.org](https://nodejs.org/) 下载安装（推荐 LTS 版本）。

### 2. ADB 工具

**Windows 用户**：项目已包含 ADB 工具，无需额外安装。

**macOS/Linux 用户**：

```bash
# macOS (使用 Homebrew)
brew install android-platform-tools

# Ubuntu/Debian
sudo apt-get install android-tools-adb

# 验证安装
adb version
```

### 3. Android 设备

- 启用开发者选项
- 启用 USB 调试
- 连接到电脑（USB 或 WiFi）

---

## 安装步骤

### 方法 1: 使用源代码（推荐开发者）

```bash
# 1. 克隆或下载项目
git clone https://github.com/pengjugame/EasyADB.git
cd EasyADB

# 2. 进入源代码目录
cd src

# 3. 安装依赖
npm install

# 4. 完成！
```

### 方法 2: 使用可执行文件（推荐普通用户）

1. 从 [Releases](https://github.com/pengjugame/EasyADB/releases) 下载最新版本
2. 解压到任意目录
3. 双击运行 `quest-video.exe` 或 `adb-manager.exe`

---

## 连接设备

### USB 连接（推荐）

1. 使用 USB 线连接设备到电脑
2. 在设备上允许 USB 调试授权
3. 验证连接：

```bash
adb devices
```

应该看到类似输出：
```
List of devices attached
1WMHH123456789  device
```

### WiFi 连接（适用于 Quest）

1. 确保设备和电脑在同一网络
2. 首次需要 USB 连接，然后执行：

```bash
# 启用 TCP/IP 模式
adb tcpip 5555

# 连接到设备（替换为你的设备 IP）
adb connect 192.168.1.100:5555
```

3. 现在可以拔掉 USB 线了

**查找 Quest 设备 IP**：
- Quest 设置 → WiFi → 点击已连接的网络 → 查看 IP 地址

---

## 第一次使用

### Quest 视频管理工具

```bash
# 进入源代码目录
cd EasyADB/src

# 运行 Quest 视频管理工具
node quest-video.js
```

**你会看到：**

```
╔════════════════════════════════════════╗
║     Meta Quest 视频管理工具 v1.0       ║
╚════════════════════════════════════════╝

🔍 正在扫描设备...
✓ 找到 15 个视频文件

┌─────────────┬──────────────────────┬──────────┐
│ 日期        │ 文件名               │ 大小     │
├─────────────┼──────────────────────┼──────────┤
│ 2026-01-23  │ video_001.mp4        │ 125.5 MB │
│ 2026-01-23  │ video_002.mp4        │ 89.2 MB  │
└─────────────┴──────────────────────┴──────────┘

? 请选择操作:
  📤 导出视频到本地
  🗑️  删除设备视频
  🧹 清理设备（保留最近X天）
  ↩️  退出
```

### 通用 ADB 文件管理器

```bash
# 运行通用文件管理器
node adb-manager.js
```

---

## 常用操作

### 1. 导出视频到本地

1. 选择 `📤 导出视频到本地`
2. 选择筛选方式：
   - `🚀 全部导出` - 导出所有文件
   - `📅 按日期筛选` - 选择特定日期
   - `📦 按应用筛选` - 选择特定应用
   - `✅ 手动勾选` - 逐个选择文件
3. 确认导出
4. 等待完成

**导出位置**：
- 默认：`E:/Quest3Videos/` （可在配置中修改）
- 按日期组织：`E:/Quest3Videos/2026-01-23/`

### 2. 删除设备文件

1. 选择 `🗑️ 删除设备视频`
2. 选择要删除的文件（同导出筛选方式）
3. **重要**：仔细检查将要删除的文件列表
4. 确认删除（输入 `y`）
5. 等待完成

**安全提示**：
- 所有删除操作都需要二次确认
- 删除后无法恢复
- 建议先导出再删除

### 3. 快速清理

1. 选择 `🧹 清理设备（保留最近X天）`
2. 选择保留时间：
   - 保留今天
   - 保留最近 3 天
   - 保留最近 7 天
   - 保留最近 14 天
   - 保留最近 30 天
   - ⚠️ 全部删除
3. 查看将要删除的文件
4. 确认删除

**使用场景**：
- Quest 存储空间不足
- 定期清理旧文件
- 保留最近的录像

### 4. 配置管理

编辑 `src/AdbFileManager/config/config.json`：

```json
{
  "presets": [
    {
      "name": "我的配置",
      "remotePath": "/sdcard/DCIM/Camera",
      "localPath": "D:/MyBackup",
      "fileTypes": [".jpg", ".mp4"],
      "recursive": false
    }
  ],
  "currentPreset": 0
}
```

---

## 故障排除

### 问题 1: 找不到 ADB 命令

**症状**：
```
❌ 找不到 ADB 命令
```

**解决方法**：
- **Windows**：确保 `platform-tools` 文件夹在项目根目录
- **macOS/Linux**：安装 Android Platform Tools 并添加到 PATH

### 问题 2: 设备未连接

**症状**：
```
❌ 未检测到设备
```

**解决方法**：
1. 检查 USB 线连接
2. 在设备上允许 USB 调试授权
3. 运行 `adb devices` 验证连接
4. 尝试重启 ADB：
   ```bash
   adb kill-server
   adb start-server
   ```

### 问题 3: 权限不足

**症状**：
```
Permission denied
```

**解决方法**：
- 确保设备上已授权 USB 调试
- 某些系统目录可能需要 root 权限
- 尝试访问其他目录（如 `/sdcard/`）

### 问题 4: 文件导出失败

**症状**：
```
导出失败: 文件名包含非法字符
```

**解决方法**：
- 检查本地保存路径是否存在
- 确保有足够的磁盘空间
- 避免使用特殊字符的文件名

### 问题 5: WiFi 连接断开

**症状**：
```
error: device offline
```

**解决方法**：
```bash
# 重新连接
adb connect 设备IP:5555

# 如果不行，重启 ADB
adb kill-server
adb start-server
adb connect 设备IP:5555
```

---

## 快捷操作

### 快速导出今天的视频

```bash
node quest-video.js
# 选择: 导出 → 按日期 → 今天 → 确认
```

### 快速清理 7 天前的文件

```bash
node quest-video.js
# 选择: 清理 → 保留最近 7 天 → 确认
```

### 快速查看设备文件

```bash
node quest-video.js
# 程序启动后会自动扫描并显示文件列表
```

---

## 进阶技巧

### 1. 批量处理

使用脚本自动化操作（需要修改代码）：

```javascript
// 示例：自动导出并删除 7 天前的文件
const cutoffDate = dayjs().subtract(7, 'day');
const oldFiles = videos.filter(v => dayjs(v.date).isBefore(cutoffDate));
await exportVideos(oldFiles);
await deleteVideos(oldFiles);
```

### 2. 自定义配置

创建多个预设配置，快速切换：

```json
{
  "presets": [
    {
      "name": "Quest 录屏",
      "remotePath": "/sdcard/oculus/VideoShots",
      "localPath": "E:/Quest3Videos"
    },
    {
      "name": "Quest 截图",
      "remotePath": "/sdcard/oculus/Screenshots",
      "localPath": "E:/Quest3Screenshots"
    }
  ]
}
```

### 3. 定时任务

**Windows**：使用任务计划程序
**macOS/Linux**：使用 cron

```bash
# 每天凌晨 2 点自动备份
0 2 * * * cd /path/to/EasyADB/src && node quest-video.js
```

---

## 下一步

- 📖 阅读[完整文档](README_CN.md)
- 🔧 查看[项目结构](PROJECT_STRUCTURE.md)
- 🤝 参与[贡献](../CONTRIBUTING.md)
- 🐛 报告[问题](https://github.com/pengjugame/EasyADB/issues)

---

## 获取帮助

- **文档**：查看 `doc/` 目录下的文档
- **问题**：在 GitHub 上创建 Issue
- **讨论**：在 GitHub Discussions 中提问

---

**祝你使用愉快！** 🎉

如果这个工具对你有帮助，请给项目一个 ⭐ Star！
